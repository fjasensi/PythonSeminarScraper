# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Mode: telegram | discord | multiple
MODE=telegram

# Discount behavior:
# - keyword (default): notify when page contains "descuento"
# - increase_over_threshold: notify only when % discount increases and is > MIN_DISCOUNT_PERCENTAGE
DISCOUNT_ALERT_MODE=keyword
MIN_DISCOUNT_PERCENTAGE=20

# Required only when MODE is discord or multiple
DISCORD_WEBHOOK_URL=
